# Chat App (Angular)

Angular 17 chat client. The UI talks to a WebSocket backend for live rooms and messages.

## What is in here

- Angular CLI app (`ng serve`)
- Chat component plus a dedicated WebSocket service
- Routing and a standard Angular workspace

## Stack

Angular 17, TypeScript, WebSockets
